load NM_Test2_FS_Mag_Phase.mat

disp('Applying Mask!...')
tic
for i = 1:96
	maskedMag(:,:,:,i) = Mask.*Mag(:,:,:,i);
end
toc
test = ADF(maskedMag,[1 1 3],10,0.5);

tic
data = ApplyADF(test);
toc;

save('ADF_results','data');
disp('Done!')
